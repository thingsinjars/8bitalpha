/**
* @description This function checks whether a given URL exists or not by sending a
* HEAD request to the URL and checking the status code returned. If the status code
* is 404 (Not Found), the function returns false (undefined).
* 
* @param { string } a - The `a` input parameter is the URL that we want to check if
* it exists or not.
* 
* @returns { boolean } The output returned by the function `UrlExists(a)` is a boolean
* value indicating whether the URL `a` exists or not.
*/
/**
* @description This function delays showing images and appends them to the HTML page
* using `setTimeout` and `append` methods.
* 
* @param { string } a - In this function `delayThenShowImages(a)`, `a` is a string
* that determines the source of the image to be displayed.
* 
* @param { number } b - The `b` input parameter is a counter that tracks the number
* of times the `setTimeout` function is called before the images are displayed.
* 
* @returns {  } This function takes two arguments `a` and `b`, and it returns no
* value (it is a void function). Here's what the function does:
* 
* 1/ It checks if a URL-style string `tmp/` + `a` exists using `UrlExists()` (this
* function is not provided). If the file does not exist (i.e., the return value of
* `UrlExists()` is false or undefined), it proceeds to step 2.
* 2/ It calls `setTimeout()` with a function call `delayThenShowImages(a++, b)` after
* 20 milliseconds. This means that if the file does not exist (i.e., `UrlExists()`
* returns false or undefined), it will wait for 20 milliseconds before checking again.
* 3/ If the file exists (i.e., `UrlExists()` returns true), it appends an `img`
* element to both `#dropzone` and `#downloadables` containing the URL of the file.
* 
* In other words., this function checks if a file with the specified name exists and
* displays an "loading" image while waiting for the file to be ready for download.
* If the file does not exist yet (i.e., it's still being uploaded), the function
* waits for a bit before checking again.
*/
function UrlExists(a){var b=new XMLHttpRequest;b.open("HEAD",a,!1),b.send();return b.status!=404}function delayThenShowImages(a,b){!UrlExists("tmp/"+a)&&b-->0?setTimeout(function(){delayThenShowImages(a,b)},20):($("#dropzone").empty().append('<img src="tmp/'+a+'">'),$("#downloadables").append('<img src="image.php?image=tmp/'+a+'">'))}$(function(){$("#dropzone").dropzone({url:"upload.php",printLogs:!0,uploadRateRefreshTime:500,numConcurrentUploads:1}),$("#questions, #about_close").click(function(){$("#about").toggle()})}),$.fn.dropzone.newFilesDropped=function(){$("#dropzone").empty().append($('<img src="images/spinner.gif" />').css({margin:"50px"})),$("#downloadables").empty()},$.fn.dropzone.uploadFinished=function(a,b,c){b.fileName.indexOf(".png")==-1?$("#downloadables").append('<img src="images/not-png.png">'):delayThenShowImages(b.fileName,10)};var _gaq=_gaq||[];_gaq.push(["_setAccount","UA-20281865-3"]),_gaq.push(["_trackPageview"]),function(){var a=document.createElement("script");a.type="text/javascript",a.async=!0,a.src=("https:"==document.location.protocol?"https://ssl":"http://www")+".google-analytics.com/ga.js";var b=document.getElementsByTagName("script")[0];b.parentNode.insertBefore(a,b)}()
